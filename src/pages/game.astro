---
import SurviveJamestown from '../components/SurviveJamestown.jsx';
import Header from '../components/header.astro';
import Leaderboard from '../components/leaderboard';
const response = await fetch('https://jamestownapush.vercel.app/api/get_leaderboard');
const data = await response.json();




if (!response.ok) {
  throw new Error('Failed to fetch leaderboard data');
}
function time (data){
  return data.Time
}
console.log(data);
data.leaderboard.sort((a,b) => a.Time- b.Time);
export const prerender = false;
---

<Header/>
<SurviveJamestown startDay={1} goalDays={10} client:load />

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Leaderboard</title>
    <style>
      /* Retro Style for Old-School Text and Clean Look */
      body {
        font-family: 'Courier New', Courier, monospace;
        background-color: #f5f5f5;
        width: 80%;
        align-items: center;
        align-self: center;
        color: #333;
        margin: 0;
        padding: 0;
        margin-left: 10%;
      }

      h1 {
        text-align: center;
        margin-top: 30px;
        font-size: 2rem;
        letter-spacing: 2px;
        color: #444;
      }

      table {
        width: 80%;
        margin: 30px auto;
        border-collapse: collapse;
        overflow-x: auto; /* Enable scrolling on small screens */
      }

      table, th, td {
        border: 1px solid #ccc;
      }

      th, td {
        padding: 10px;
        text-align: left;
      }

      th {
        background-color: #f2f2f2;
        font-weight: bold;
        text-transform: uppercase;
      }

      tr:nth-child(odd) {
        background-color: #f9f9f9;
      }

      tr:hover {
        background-color: #e1e1e1;
      }

      .leaderboard-table {
        width: 100%;
      }

      .rank {
        text-align: center;
      }

      .score {
        text-align: center;
      }

      footer {
        text-align: center;
        font-size: 0.9rem;
        color: #777;
        margin-top: 30px;
      }

      /* Mobile Responsiveness */
      @media screen and (max-width: 768px) {
        body {
          width: 95%;
          margin-left: 2.5%;
        }

        h1 {
          font-size: 1.5rem; /* Smaller header on mobile */
        }

        table {
          width: 100%; /* Make table full width on mobile */
        }

        th, td {
          padding: 8px; /* Slightly smaller padding for mobile */
        }

        footer {
          font-size: 0.8rem;
        }

        .leaderboard-table {
          font-size: 0.9rem; /* Slightly smaller font size for better readability */
        }
      }

      /* Very small mobile screens */
      @media screen and (max-width: 480px) {
        h1 {
          font-size: 1.2rem;
        }

        table {
          font-size: 0.8rem; /* Smaller table font size on very small screens */
        }

        th, td {
          padding: 6px; /* Even smaller padding on very small screens */
        }
      }
    </style>
  </head>

  <body>
    <h1>Leaderboard</h1>
    <table class="leaderboard-table">
      <thead>
        <tr>
          <th class="rank">Rank</th>
          <th>Player Name</th>
          <th class="score">Score</th>
          <th>Time</th>
        </tr>
      </thead>
      <tbody>
        {data.leaderboard.map((entry, index) => (
          <tr key={entry.id}>
            <td class="rank">{index + 1}</td>
            <td>{entry.player_name}</td>
            <td class="score">{entry.score}</td>
            <td>{entry.Time}</td>
          </tr>
        ))}
      </tbody>
    </table>

    <footer>
      <p>&copy; 2025 Sreek. No a single on of the rights reserved. Code available here : <a href="https://github.com/TheMerjin/jamestown" target="_blank">GitHub</a></p>
    </footer>
  </body>